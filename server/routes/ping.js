/*import Ping from 'hapi-alive'

async function pingHandler(request, h) {
    return h.response({ message: 'ok', request }).code(200);
}

const pingRoutes = [{
    method: 'GET',
    path: '/ping',
    handler: pingHandler()
}];

export default { pingRoutes };
*/